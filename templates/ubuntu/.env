# Ubuntu 22.04 Development Environment Configuration

# =============================================================================
# Build-time Configuration
# =============================================================================

# Base system version
UBUNTU_VERSION=22.04
UBUNTU_NAME=2204

# Docker 镜像源配置 - 中国优化版本
# 基础镜像仓库地址 (影响 FROM 指令的镜像下载速度)
# 可用加速器（测试通过）:
# - docker.1ms.run（备选）
# - doublezonline.cloud（备选）
# - docker.m.daocloud.io（DaoCloud官方）
# - docker.io（Docker Hub原址，中国不可用或较慢）
DOCKER_REGISTRY=docker.1ms.run

# Git version
GIT_VERSION=latest

# System dependencies
DEPENDENCIES="curl git wget ssh build-essential ca-certificates"

# =============================================================================
# Runtime Configuration
# =============================================================================

# Project configuration
PROJECT_NAME=ubuntu
WORKSPACE_PATH=/workspace

# Container resource limits
MEMORY_LIMIT=2g
CPU_LIMIT=2
SHM_SIZE=512m